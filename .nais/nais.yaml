apiVersion: nais.io/v1alpha1
kind: Application
metadata:
  name: dp-arena-meldeplikt-adapter
  namespace: teamdagpenger
  labels:
    team: teamdagpenger
spec:
  image: {{ image }}
  port: 8080
  prometheus:
    enabled: true
    path: /internal/metrics
  liveness:
    path: /internal/isalive
  readiness:
    path: /internal/isready
  replicas:
    min: 2
    max: 4
  resources:
    limits:
      memory: 512Mi
    requests:
      cpu: 200m
      memory: 256Mi
  ingresses:
    {{#each ingresses as |url|}}
     - {{ url }}
    {{/each}}
  secureLogs:
    enabled: true
  env:
    - name: DP_PROXY_URL
      value: "https://{{ dp_proxy.ingress }}/proxy/v2/arenaords"
    - name: DP_PROXY_SCOPE
      value: {{ dp_proxy.scope }}
  tokenx:
    enabled: true
  azure:
    application:
      enabled: true
  accessPolicy:
    inbound:
      rules:
        - application: dp-rapportering
    outbound:
      external:
        - host: {{ dp_proxy.ingress }}
